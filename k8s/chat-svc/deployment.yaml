apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-svc
  namespace: chat-app
  labels:
    app: chat-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat-svc
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: chat-svc
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: chat-svc
          image: us-central1-docker.pkg.dev/microservices-chat-496017/chat-platform-cluster-registry/chat-svc:ee7e20f01c72ddb2bb1ac13993c67a611b801d04
          ports:
            - containerPort: 8080
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: chat-db-secret
                  key: SECRET_KEY
            - name: CHAT_DB_HOST
              valueFrom:
                secretKeyRef:
                  name: chat-db-secret
                  key: CHAT_DB_HOST
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
