apiVersion: v1
kind: Service
metadata:
  name: chat-svc
  namespace: chat-app
  labels:
    app: chat-svc
spec:
  type: ClusterIP
  selector:
    app: chat-svc
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
