apiVersion: v1
kind: Service
metadata:
  name: chat-db
  namespace: chat-app
  labels:
    app: chat-db
spec:
  type: ClusterIP
  selector:
    app: chat-db
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
