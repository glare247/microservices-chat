apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-db
  namespace: chat-app
  labels:
    app: chat-db
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat-db
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: chat-db
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: chat-db
          image: us-central1-docker.pkg.dev/microservices-chat-496017/chat-platform-cluster-registry/chat-db:02230bf69e7286e493febb5713bffc50beeffcd3
          ports:
            - containerPort: 8080
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: chat-db-secret
                  key: SECRET_KEY
            - name: SQLALCHEMY_DATABASE_URI
              valueFrom:
                secretKeyRef:
                  name: chat-db-secret
                  key: SQLALCHEMY_DATABASE_URI
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
